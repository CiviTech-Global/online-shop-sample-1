<div dir="rtl" align="right">

# قوانین و سیستم طراحی فروشگاه آنلاین فارسی

> **پروژه:** فروشگاه آنلاین فارسی‌زبان  
> **تاریخ:** مرداد ۱۴۰۵ / اوت ۲۰۲۶  
> **هدف:** تعریف قواعد UI/UX، تایپوگرافی، رنگ، RTL، انیمیشن و دسترسی‌پذیری

---

## ۱. جهت‌گیری و RTL

- صفحه باید `dir="rtl"` و `lang="fa"` داشته باشد.
- تمام متون به‌صورت پیش‌فرض `text-align: right` باشند.
- از ویژگی‌های منطقی CSS استفاده شود:
  - `margin-inline-start` به جای `margin-left`
  - `padding-inline-end` به جای `padding-right`
  - `border-inline-start` به جای `border-left`
- آیکون‌های جهت‌دار (فلش، چشمک) در RTL باید آینه شوند.
- scrollbarها در WebKit قابل تنظیم و RTL-friendly باشند.

---

## ۲. تایپوگرافی

### ۲.۱ فونت پیشنهادی

- **فونت اصلی:** `Vazirmatn` (نسخه Variable یا woff2)
- **CDN:** `https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v33.003/Vazirmatn-font-face.css`
- **فونت اعداد:** همان Vazirmatn با `font-variant-numeric: tabular-nums`
- **فونت جایگزین:** `Shabnam` یا `Sahel`

### ۲.۲ مقیاس تایپوگرافی

| عنصر | اندازه | وزن | کاربرد |
|---:|---:|---:|---|
| H1 | ۲.۲۵rem (36px) | 700 | عنوان صفحه اصلی/لندیگ |
| H2 | ۱.۸۷۵rem (30px) | 700 | عنوان بخش |
| H3 | ۱.۵rem (24px) | 600 | عنوان زیربخش |
| Body | 1rem (16px) | 400 | متن اصلی |
| Small | 0.875rem (14px) | 400 | کپشن، قیمت‌ها |
| Caption | 0.75rem (12px) | 400 | برچسب‌ها |

- اعداد قیمت با `tabular-nums` برای تراز یکنواخت.
- ارتفاع خط (line-height) برای متن فارسی: ۱.۷ تا ۱.۹.

---

## ۳. پالت رنگی

### ۳.۱ رنگ‌های اصلی پیشنهادی

| نام | کد | کاربرد |
|---:|---:|---|
| Primary | `#E63946` | دکمه اصلی، تخفیف، هایلایت |
| Primary Dark | `#C62828` | hover دکمه اصلی |
| Secondary | `#1D3557` | هدر، فوتر، متن‌های مهم |
| Accent | `#F4A261` | بنر، CTA ثانویه |
| Success | `#2A9D8F` | موجودی، پیام موفق |
| Background | `#F8F9FA` | پس‌زمینه صفحه |
| Surface | `#FFFFFF` | کارت‌ها، پنل‌ها |
| Text Primary | `#212529` | متن اصلی |
| Text Secondary | `#6C757D` | متن فرعی |
| Border | `#DEE2E6` | خطوط و حاشیه‌ها |

### ۳.۲ رنگ‌های حالت تاریک (Dark Mode)

| عنصر | کد |
|---:|---:|
| پس‌زمینه | `#121212` |
| سطح | `#1E1E1E` |
| متن اصلی | `#F8F9FA` |
| متن فرعی | `#ADB5BD` |

---

## ۴. چیدمان و فاصله‌گذاری

### ۴.۱ Grid و Container

- Container حداکثر: `1280px` (xl)، `1024px` (lg)
- Padding افقی: `1rem` موبایل، `1.5rem` تبلت، `2rem` دسکتاپ
- گرید محصولات: ۲ ستون موبایل، ۳ تبلت، ۴ دسکتاپ، ۵ دسکتاپ بزرگ
- فاصله بین کارت‌ها (gap): `1rem` تا `1.5rem`

### ۴.۲ Border Radius

| عنصر | مقدار |
|---:|---:|
| کارت محصول | `12px` تا `16px` |
| دکمه‌ها | `8px` تا `9999px` (pill) |
| ورودی‌ها | `8px` تا `12px` |
| بنرها | `16px` تا `24px` |

---

## ۵. کامپوننت‌ها

### ۵.۱ دکمه‌ها

- دکمه اصلی: پس‌زمینه Primary، متن سفید، border-radius pill
- دکمه ثانویه: پس‌زمینه شفاف، border Primary
- دکمه آیکون: گرد، سایه نرم، hover scale
- همه دکمه‌ها باید focus-visible داشته باشند.

### ۵.۲ کارت محصول

- سایه نرم: `0 4px 12px rgba(0,0,0,0.06)`
- hover: افزایش سایه + scale تصویر ۱.۰۵
- badge تخفیف: گوشه بالا سمت راست (RTL)
- دکمه افزودن به سبد: پایین کارت

### ۵.۳ فرم‌ها

- Label بالای input، تراز راست
- Placeholder راست‌چین
- Validation با پیام فارسی
- focus state با ring رنگ Primary

---

## ۶. انیمیشن و موشن

### ۶.۱ اصول کلی

- انیمیشن‌ها باید **هدفمند** باشند (راهنمایی، بازخورد، لذت بصری).
- مدت زمان استاندارد: `150ms` تا `300ms`
- easing: `cubic-bezier(0.4, 0, 0.2, 1)`
- از `transform` و `opacity` استفاده شود تا Performance بالا باشد.
- در `prefers-reduced-motion` انیمیشن‌ها کاهش یابند.

### ۶.۲ میکرواینتراکشن‌ها

| تعامل | اثر |
|---|---|
| hover کارت | `translateY(-4px)` + سایه بزرگ‌تر |
| hover تصویر | `scale(1.05)` |
| click دکمه | `scale(0.97)` |
| افزودن به سبد | shake کوتاه + toast |
| اسکرول به بخش | smooth scroll |

### ۶.۳ اسکرول‌محور

- fade-in-up برای کارت‌ها هنگام ورود به viewport
- stagger delay بین آیتم‌های لیست
- parallax ملایم روی بنرهای hero

### ۶.۴ انتقال صفحات

- fade یا slide برای تب‌ها
- skeleton loading هنگام بارگذاری محصولات

---

## ۷. دسترسی‌پذیری (Accessibility)

- کنتراست رنگ حداقل ۴.۵:۱
- پشتیبانی از keyboard navigation
- `aria-label` برای آیکون‌ها
- `alt` برای تمام تصاویر
- focus indicator واضح
- سایز فونت قابل تغییر
- پشتیبانی از screen reader

---

## ۸. Responsive Breakpoints

| نام | عرض | تغییرات |
|---:|---:|---|
| sm | 640px | گوشی‌های بزرگ |
| md | 768px | تبلت |
| lg | 1024px | لپ‌تاپ |
| xl | 1280px | دسکتاپ |
| 2xl | 1536px | دسکتاپ بزرگ |

---

## ۹. زبان و محتوا

- زبان رابط: فارسی
- اعداد: فارسی/انگلیسی با قابلیت تنظیم
- تاریخ: شمسی (در صورت امکان)
- واحد پول: تومان (پیش‌فرض)
- متن‌ها کوتاه، واضح و بدون غلط املایی

</div>

---

## ۱۰. پژوهش UI/UX فروشگاه‌های فارسی (۱۴۰۵)

### ۱۰.۱ الگوهای برتر تبدیل (Conversion)

- **اعتماد اولیه در بالای صفحه:** نشان‌های اعتماد، تضمین اصالت و شماره پشتیبانی در هدر یا بالای Hero.
- **CTA شفاف:** دکمه «افزودن به سبد خرید» با رنگ متضاد (Primary) و اندازه بزرگ در کارت و صفحه محصول.
- **فوریت مصنوعی:** نمایش موجودی محدود، تعداد بازدیدکنندگان اخیر یا زمان باقی‌مانده تخفیف.
- **جستجوی هوشمند:** پیشنهاد لحظه‌ای، تصحیح املا و فیلتر قیمت/دسته‌بندی در صفحه نتایج.
- **فرم تک‌مرحله‌ای checkout:** هرچه تعداد مراحل کمتر باشد نرخ رها شدن کمتر می‌شود.

### ۱۰.۲ سیکولوژی خرید آنلاین در ایران

- **قیمت شفاف:** نمایش قیمت اصلی، تخفیف و قیمت نهایی به‌صورت ممتاز و بدون پنهان‌کاری.
- **هزینه ارسال زودهنگام:** نمایش تقریبی هزینه ارسال در صفحه محصول یا سبد خرید.
- **پشتیبانی قابل دسترس:** دکمه تماس/واتس‌اپ در موبایل و فوتر.
- **نظرات و امتیازات:** نمایش امتیاز ستاره‌ای و تعداد نظرات در کارت محصول.
- **تسهیل بازگشت:** اشاره به «۷ روز ضمانت بازگشت» نزدیک دکمه خرید.

### ۱۰.۳ دسترسی‌پذیری پیشرفته

- **اندازه هدف لمسی:** حداقل ۴۴×۴۴ پیکسل برای دکمه‌ها در موبایل.
- **برچسب‌های فرم:** هر input دارای label مرتبط با `htmlFor` باشد.
- **اعلان‌های غیرزبانی:** toastها همراه با آیکون واضح (موفقیت/خطا/هشدار).
- **کنتراست بالا:** رنگ Primary روی Background نسبت حداقل ۴.۵:۱.
- **فوکوس منطقی:** ترتیب tab از راست به چپ و از بالا به پایین در RTL.

### ۱۰.۴ میکرواینتراکشن‌های کلیدی

- **افزودن به سبد:** انیمیشن محصول به سمت آیکون سبد + به‌روزرسانی شمارنده.
- **لایک/علاقه‌مندی:** ضربان قلب کوتاه هنگام ذخیره محصول.
- **اسکلتون لودینگ:** به جای spinner چرخان برای کارت‌ها و لیست‌ها.
- **هایلایت تغییر قیمت:** چشمک زدن رنگ سبز/قرمز هنگام اعمال کد تخفیف.
- **هوور محصول:** افزایش سایه و بزرگنمایی تصویر با `will-change: transform`.

---

## ۱۱. سئو (SEO) و کد semantics

### ۱۱.۱ مدیریت متای صفحات

- استفاده از `react-helmet-async` برای تگ `<title>` و `<meta name="description">` هر صفحه.
- تگ `<html lang="fa" dir="rtl">` در `index.html`.
- canonical URL منحصربه‌فرد برای هر صفحه جلوگیری از duplicate content.

### ۱۱.۲ Open Graph و Twitter Card

- `og:title`، `og:description`، `og:image`، `og:url` و `og:locale="fa_IR"` برای تمام صفحات.
- تصویر OG پیش‌فرض در ریشه public یا تصویر اصلی محصول در صفحه محصول.

### ۱۱.۳ Structured Data

- صفحه اصلی: `WebSite` با `SearchAction`.
- صفحه محصول: `Product` شامل نام، توضیحات، تصویر، SKU، `Offers` با قیمت و موجودی.
- صفحه دسته‌بندی: `ItemList` / `BreadcrumbList`.

### ۱۱.۴ فایل‌های استاتیک سئو

- `/robots.txt`: اجازه خزش به همه ربات‌ها و اشاره به sitemap.
- `/sitemap.xml`: تولید داینامیک در بک‌اند از روی محصولات، دسته‌بندی‌ها و صفحات استاتیک.
- semantic HTML: استفاده از `<header>`، `<nav>`، `<main>`، `<section>`، `<article>`، `<footer>`.

### ۱۱.۵ بهینه‌سازی تصاویر

- alt توصیفی برای تصاویر محصول با کلمات کلیدی طبیعی.
- lazy loading برای تصاویر خارج از viewport.
- استفاده از فرمت WebP/AVIF در صورت امکان.

---

## ۱۲. پیامک و ارتباط با مشتری

### ۱۲.۱ قالب‌های پیامکی

- ذخیره قالب‌ها در دیتابیس با متغیرهای قابل تنظیم (`{{orderNumber}}`، `{{status}}`، ...).
- امکان ویرایش قالب توسط ادمین بدون تغییر کد.
- نمایش پیش‌نمایش متن قبل از ارسال انبوه.

### ۱۲.۲ سناریوهای کلیدی

- **ثبت سفارش:** ارسال خودکار پس از تکمیل خرید با شماره سفارش و مبلغ.
- **تغییر وضعیت سفارش:** اطلاع‌رسانی مراحل پردازش، ارسال و تحویل.
- **سبد خرید رها شده:** ارسال یادآوری به کاربران دارای سبد غیرفعال بیش از X ساعت.
- **کد تخفیف:** ارسال کد ویژه به مشتریان وفادار.

### ۱۲.۳ شبیه‌ساز (Simulator)

- در محیط توسعه و زمانی که کلید SMS provider تنظیم نشده، پیامک در دیتابیس ثبت و در پنل نمایش داده می‌شود.
- لاگ کامل شامل گیرنده، قالب، وضعیت، زمان ارسال و پاسخ provider.

---

## ۱۳. عملکرد (Performance)

- Code-splitting صفحات ادمین و فرم‌های سنگین با `React.lazy`.
- بهینه‌سازی font: استفاده از زیرمجموعه فونت Vazirmatn و `font-display: swap`.
- کش کوئری‌ها با React Query و staleTime مناسب برای تنظیمات و دسته‌بندی‌ها.
- کاهش ریکوئست‌های API: بارگذاری تنظیمات عمومی یک‌باره در `<Seo>` و layout.
- Image CDN یا placeholder برای تصاویر محصول فاقد تصویر.

---

## ۱۴. چک‌لیست تحویل Foundation Deal

- [ ] RTL، Vazirmatn و semantic HTML.
- [ ] مدیریت meta/OG/JSON-LD برای صفحات اصلی.
- [ ] sitemap.xml و robots.txt فعال.
- [ ] سبد خرید، checkout و درگاه پرداخت تست شده.
- [ ] پنل ادمین با مدیریت محصولات، سفارش‌ها، تنظیمات و CMS.
- [ ] ماژول پیامک با قالب‌ها، شبیه‌ساز و بازیابی سبد رها شده.
- [ ] build و lint بدون خطا.

</div>
